<!DOCTYPE html>
<html>
<head>
	<title>Blog</title>
	<%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
	<%= javascript_include_tag "application", "data-turbolinks-track" => true %>
	<%= csrf_meta_tags %>
</head>



<body data-twttr-rendered="true">
	<aside id='default-popup' class='avgrund-popup undefined'>
	<h2>hello there</h2>
	<button id='close_btn'>Close</button>
</aside>
  
  <div id='containter' class='avgrund-contents'>
	<% flash.each do |name, msg| %>
	  <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
	<% end %>
	  <% #link_to "My Profile", user_root_path%>
	  <nav class="navbar navbar-default navbar-static-top" role="navigation">
	  	<div class='header_name'><%= link_to('SimplizIT', root_path, method: 'get') %></div>
	  	<% if current_user %>
	  	  <div class='active_user'>&nbsp; &nbsp; — &nbsp; Welcome... <span><b><%= current_user.username %></b></span></div>
	  	<% end %>
	  	<ul class="nav">
        <% if current_user %> 
          <li><%= link_to('Sign Out', destroy_user_session_path, :method=>'delete') %></li>
    	  <% else %>
          <li><%= link_to('Sign In', new_user_session_path, :method=>'get') %></li>
    	    <li><%= link_to('Sign Up', new_user_registration_path, :method=>'get') %></li>
     	  <% end %>
      </ul>
     </nav>	  
	<%= yield %>
</body>

</html>


